package katas.kyu7;

public class SmokingTimmy {
    public static int startSmoking(int bars, int boxes) {
        int cig = bars * 180 + boxes * 18;
        int newCigs = cig/5;
        while (newCigs > 0){
            cig += newCigs;
            new
        }

        return cig + cig / 5;
    }
}